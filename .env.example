# GraphQL API Configuration
GRAPHQL_URI=https://api.example.com/graphql

# Login Credentials
USERNAME=your_username_here
PASSWORD=your_password_here

# Optional: API Token (if you already have one)
# AUTH_TOKEN=your_bearer_token_here
